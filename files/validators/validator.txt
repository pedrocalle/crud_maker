import { yup } from '@/lib';
import { NotRequiredValidator } from '@/shared/validators';

export const CreateuModuleName = yup.object({
create
});

export const UpdateuModuleName = yup.object({
  ...NotRequiredValidator(CreateuModuleName),
  id: yup.string().uuid().required(),
});